import React from "react";

interface MainProps {
  windowWidth: number;
}

const Main: React.FC<MainProps> = ({ windowWidth }) => {
  return (
    <>
      <div>★</div>
    </>
  );
};

export default Main;
